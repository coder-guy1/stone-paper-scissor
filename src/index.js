import React from "react";
import ReactDOM from "react-dom";
import "./game.css"; // Import your global styles if any
import App from "./App"; // Import your root component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
